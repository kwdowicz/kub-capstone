apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-server-ingress
  namespace: policy-smoke
spec:
  podSelector:
    matchLabels:
      app: policy-server
  policyTypes:
    - Ingress
